
---------------------------------------------------------------------
[TO RUN DEMO]
---------------------------------------------------------------------
make zmqdemo
<also run the QueryServer separately using ./QueryServer>


---------------------------------------------------------------------
[PARAMETERS AVAILABLE FOR DEMO]
---------------------------------------------------------------------
  Note: Default values are usable for demo

n    --> [default:   10] number of OS-processes
nfg  --> [default:   10] number of processes inside an OS-process
b    --> [default:  100] batchsize threshold used for batched implementations
w    --> [default:   10] window size for applications to make outstanding requests   [ideally equal to 'nfg'] [should be >=1]
k    --> [default:   20] number of requests sent eagerly by list nodes to its neighbors [ideally greater and close to 'w'] [should be >=0]
IP   --> [default:    0] INSERT probability
DP   --> [default:    0] DELETE probability
RFP  --> [default:    0] RANGE-QUERY probability (Note: that the rest of the probability is for FINDS)
AS   --> [default:    1] Should we use APP shortcuts
LS   --> [default:    0] Should we use LIST shortcuts
MD   --> [default:    1] Dimension for Range Query
SP   --> [default:  100] Span for the Range Query to be used
TP   --> [default:   50] Probability used to toss coins when generating tower height via binomial process
LT   --> [default:    0] Latency Threshold to increase or decrease Window Limit,W, in app processes. [0 means always maximize W]
ITER --> [default: 1000] Number of iterations before an MPI_Wtime() is called. [If higher, experiment time will be longer but efficient]

  Example: (to show how to specify parameter (order does not matter) (if parameter not set, default is used))
    make zmqdemo n=5 nfg=100 b=100 



